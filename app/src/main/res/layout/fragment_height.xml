<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/gray_light">

        <TextView
            android:id="@+id/tv_title_mail"
            android:text="@string/register_height_title"
            style="@style/register_titles"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <EditText
            android:id="@+id/et_height"
            style="@style/edit_text_register"
            android:inputType="number"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_title_mail">

        </EditText>

        <TextView
            android:id="@+id/tv_cm_symbol"
            style="@style/symbol_register"
            android:text="@string/cm_symbol"
            app:layout_constraintStart_toEndOf="@id/et_height"
            app:layout_constraintTop_toTopOf="@id/et_height"
            app:layout_constraintBottom_toBottomOf="@id/et_height"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>